# Troubleshooting

## Configuration errors

- Ensure all required keys exist in your YAML file.
- Validate configuration with `viral-pipeline config --validate`.

## Input FASTQ issues

- Ensure `.fastq` or `.fq` extensions.
- Verify read lengths meet minimum thresholds.
